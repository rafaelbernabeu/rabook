# Manage tar files

Compress (recurvise by default):
-c create
-z gzip
-f (file)
```
$ tar -czf projects.tar.gz Projects/
```

Decompress:
-x extract
-z gzip
-f (file)
```
$ tar -xzf projetos.tar.gz
```
